{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "vite": true,
  "vitest": true,
  "playwright": true,
  // Docker copies this native binary; Python starts it without importing its JS launcher.
  "ignoreDependencies": ["y-sweet"],
  "ignoreUnresolved": [
    // Shell scripts invoked at runtime via spawnSync/execFileSync from specs, not module imports.
    "./tools/env.sh",
    "./tools/dev/pwa.sh",
    "./tools/prod/docker.sh"
  ],
  // Test-visible exports that are also used inside their production module are
  // live implementation details, not dead production code.
  "ignoreExportsUsedInFile": true,
  "entry": [
    // Reference code retained for docs/todo.md#cross-server-linking-redesign.
    "src/server/{app,auth/auth,db/client,documents/document-registry}.ts",
    // Snapshot tooling still has consumers; the Node backup exporter is reference
    // code, not a supported Django command. See docs/todo.md#operations.
    "tools/snapshot/cli.ts!",
    "tools/snapshot/backup.ts",
    // Retained source-linking fixture for the same post-migration redesign.
    "playwright.docker.config.ts",
    "tests/e2e/docker/_support/seed-source-user.ts",
    "tools/{check-pnpm-policy,collab-server,generate-api-client}.ts",
    ".agents/skills/remdo-verify-change/tools/run-reviews.ts",
    // Django's launcher consumes the server configuration projection.
    "tools/dev/**/*.ts",
    "tests/perf/**/*.bench.ts",
    "tests/perf/generate-workload.ts",
    "vitest.bench.perf.config.mts"
  ],
  "project": [
    // `!` marks the production graph used by `knip --production`.
    "src/**/*.{ts,tsx,mts}!",
    "!src/**/*.spec.{ts,tsx,mts}!",
    // Retained backend and shared helpers have reference-only exports.
    // Full-mode auditing covers them; live snapshot imports still resolve their dependencies.
    "!src/server/**!",
    "!src/domain/documents/special-documents.ts!",
    "!src/projection/yjs-ordered-map-array.ts!",
    "!src/platform/http/status.ts!",
    "!src/platform/net/http-origin.ts!",
    "config/{index.ts,env/**/*.ts}!",
    "tests/**/*.{ts,tsx,mts}",
    "config/{_internal,eslint,playwright,vite}/**/*.{ts,tsx,mts}",
    "tools/lib/**/*.{ts,tsx,mts}",
    ".agents/skills/**/*.{ts,tsx,mts}",
    "*.{ts,mts}"
  ]
}
