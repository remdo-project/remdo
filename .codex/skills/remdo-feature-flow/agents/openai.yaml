interface:
  display_name: "RemDo Feature Flow"
  short_description: "Codex adapter for RemDo feature flow"
  default_prompt: "Use $remdo-feature-flow to run the RemDo feature workflow with Codex subagent, review, and verification adapters."
policy:
  allow_implicit_invocation: false
  subagents:
    authorized: true
    use_cases:
      - "required fresh-context reads"
      - "independent Research or Phase-4 work"
