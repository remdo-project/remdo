diff --git a/dist/Lexical.dev.js b/dist/Lexical.dev.js
index 9ed00b4737d4c5a6557b74bc1335e72c84905d57..41913080d13fac6a4e848aa6293309b84c086dee 100644
--- a/dist/Lexical.dev.js
+++ b/dist/Lexical.dev.js
@@ -16855,7 +16855,7 @@ function updateEditorSync(editor, updateFn, options) {
       // routed to editor._onError rather than rethrown, so the mutation is
       // silently dropped. Route through $beginUpdate instead, which starts a
       // fresh writable update, so the work actually applies.
-      {
+      if (!isCommittingPendingUpdates || isReadOnlyMode) {
         console.warn(`updateEditorSync: an editor update (e.g. a command listener that ` + `mutates the editor) ran while a read-only context was on the ` + `stack. This most commonly happens when a command is dispatched ` + `from inside editor.read(). The update has been deferred to a ` + `fresh writable update so it still applies, but dispatching ` + `mutations from a read-only context is an anti-pattern — dispatch ` + `after editor.read() returns, or via queueMicrotask.`);
       }
       $beginUpdate(editor, updateFn, options);
diff --git a/src/LexicalUpdates.ts b/src/LexicalUpdates.ts
index 1d491b92015750569011a9f4a16cc89618694b41..573b9b236c4c2a9efd557cf87aba7d9ba3c095f5 100644
--- a/src/LexicalUpdates.ts
+++ b/src/LexicalUpdates.ts
@@ -1335,7 +1335,7 @@ export function updateEditorSync(
       // routed to editor._onError rather than rethrown, so the mutation is
       // silently dropped. Route through $beginUpdate instead, which starts a
       // fresh writable update, so the work actually applies.
-      if (__DEV__) {
+      if (__DEV__ && (!isCommittingPendingUpdates || isReadOnlyMode)) {
         console.warn(
           `updateEditorSync: an editor update (e.g. a command listener that ` +
             `mutates the editor) ran while a read-only context was on the ` +
