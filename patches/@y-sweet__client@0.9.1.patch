diff --git a/dist/main.js b/dist/main.js
index 5d0a238cc12a5258879bdcfb48ce3e42cf70c0bc..d15867e9cb19ec612d21b57bf3ff42974b7d7bc7 100644
--- a/dist/main.js
+++ b/dist/main.js
@@ -547,6 +547,7 @@ var YSweetProvider2 = class {
     this.ackedVersion = -1;
     /** Whether we are currently in the process of connecting. */
     this.isConnecting = false;
+    this.connectionGeneration = 0;
     this.heartbeatHandle = null;
     this.connectionTimeoutHandle = null;
     this.reconnectSleeper = null;
@@ -702,7 +703,10 @@ var YSweetProvider2 = class {
   }
   async ensureClientToken() {
     if (this.clientToken === null) {
-      this.clientToken = await getClientToken(this.authEndpoint, this.docId);
+      const generation = this.connectionGeneration;
+      const token = await getClientToken(this.authEndpoint, this.docId);
+      if (generation !== this.connectionGeneration) return token;
+      this.clientToken = token;
     }
     return this.clientToken;
   }
@@ -716,7 +720,7 @@ var YSweetProvider2 = class {
         if (event === STATUS_CONNECTED) {
           this.off(EVENT_CONNECTION_STATUS, statusListener);
           resolve(true);
-        } else if (event === STATUS_ERROR) {
+        } else if (event === STATUS_ERROR || event === STATUS_OFFLINE) {
           this.off(EVENT_CONNECTION_STATUS, statusListener);
           resolve(false);
         }
@@ -735,15 +739,17 @@ var YSweetProvider2 = class {
       return;
     }
     this.isConnecting = true;
+    const generation = this.connectionGeneration;
     this.setStatus(STATUS_CONNECTING);
     const lastDebugUrl = this.debugUrl;
     connecting:
-      while (![STATUS_OFFLINE, STATUS_CONNECTED].includes(this.status)) {
+      while (generation === this.connectionGeneration && ![STATUS_OFFLINE, STATUS_CONNECTED].includes(this.status)) {
         this.setStatus(STATUS_CONNECTING);
         let clientToken;
         try {
           clientToken = await this.ensureClientToken();
         } catch (e) {
+          if (generation !== this.connectionGeneration) return;
           console.warn("Failed to get client token", e);
           this.setStatus(STATUS_ERROR);
           let timeout = DELAY_MS_BEFORE_RETRY_TOKEN_REFRESH * Math.min(MAX_BACKOFF_COEFFICIENT, Math.pow(BACKOFF_BASE, this.retries));
@@ -753,7 +759,10 @@ var YSweetProvider2 = class {
           continue;
         }
         for (let i = 0; i < RETRIES_BEFORE_TOKEN_REFRESH; i++) {
-          if (await this.attemptToConnect(clientToken)) {
+          if (generation !== this.connectionGeneration) return;
+          const connected = await this.attemptToConnect(clientToken);
+          if (generation !== this.connectionGeneration) return;
+          if (connected) {
             this.retries = 0;
             break connecting;
           }
@@ -762,8 +771,10 @@ var YSweetProvider2 = class {
           this.reconnectSleeper = new Sleeper(timeout);
           await this.reconnectSleeper.sleep();
         }
+        if (generation !== this.connectionGeneration) return;
         this.clientToken = null;
       }
+    if (generation !== this.connectionGeneration) return;
     this.isConnecting = false;
     if (this.showDebuggerLink && lastDebugUrl !== this.debugUrl) {
       console.log(
@@ -777,10 +788,37 @@ var YSweetProvider2 = class {
     }
   }
   disconnect() {
-    this.setStatus(STATUS_OFFLINE);
+    this.connectionGeneration += 1;
+    this.isConnecting = false;
+    this.clearHeartbeat();
+    this.clearConnectionTimeout();
+    this.reconnectSleeper?.wake();
     if (this.websocket) {
-      this.websocket.close();
+      const websocket = this.websocket;
+      this.websocket = null;
+      websocket.onmessage = null;
+      websocket.onclose = null;
+      websocket.onerror = () => {};
+      // Chromium warns if close() interrupts the opening handshake. Detach now,
+      // then close this socket on open without running the provider's handshake.
+      if (typeof window !== "undefined" && websocket.readyState === this.WebSocketPolyfill.CONNECTING) {
+        websocket.onopen = () => {
+          websocket.onopen = null;
+          websocket.close();
+        };
+      } else {
+        websocket.onopen = null;
+        websocket.close();
+      }
     }
+    this.setStatus(STATUS_OFFLINE);
+    awarenessProtocol.removeAwarenessStates(
+      this.awareness,
+      Array.from(this.awareness.getStates().keys()).filter(
+        (client) => client !== this.doc.clientID
+      ),
+      this
+    );
   }
   bindWebsocket(websocket) {
     if (this.websocket) {
@@ -926,9 +964,7 @@ var YSweetProvider2 = class {
     this.send(toUint8Array(encoder));
   }
   destroy() {
-    if (this.websocket) {
-      this.websocket.close();
-    }
+    this.disconnect();
     if (this.indexedDBProvider) {
       this.indexedDBProvider.destroy();
     }
diff --git a/dist/main.mjs b/dist/main.mjs
index 0f0582d7679316c79f57bcfddf55c21af64fd806..4ea5c1d25072995cad1410bcee7d5fb51addf53a 100644
--- a/dist/main.mjs
+++ b/dist/main.mjs
@@ -547,6 +547,7 @@ var YSweetProvider2 = class {
     this.ackedVersion = -1;
     /** Whether we are currently in the process of connecting. */
     this.isConnecting = false;
+    this.connectionGeneration = 0;
     this.heartbeatHandle = null;
     this.connectionTimeoutHandle = null;
     this.reconnectSleeper = null;
@@ -702,7 +703,10 @@ var YSweetProvider2 = class {
   }
   async ensureClientToken() {
     if (this.clientToken === null) {
-      this.clientToken = await getClientToken(this.authEndpoint, this.docId);
+      const generation = this.connectionGeneration;
+      const token = await getClientToken(this.authEndpoint, this.docId);
+      if (generation !== this.connectionGeneration) return token;
+      this.clientToken = token;
     }
     return this.clientToken;
   }
@@ -716,7 +720,7 @@ var YSweetProvider2 = class {
         if (event === STATUS_CONNECTED) {
           this.off(EVENT_CONNECTION_STATUS, statusListener);
           resolve(true);
-        } else if (event === STATUS_ERROR) {
+        } else if (event === STATUS_ERROR || event === STATUS_OFFLINE) {
           this.off(EVENT_CONNECTION_STATUS, statusListener);
           resolve(false);
         }
@@ -735,15 +739,17 @@ var YSweetProvider2 = class {
       return;
     }
     this.isConnecting = true;
+    const generation = this.connectionGeneration;
     this.setStatus(STATUS_CONNECTING);
     const lastDebugUrl = this.debugUrl;
     connecting:
-      while (![STATUS_OFFLINE, STATUS_CONNECTED].includes(this.status)) {
+      while (generation === this.connectionGeneration && ![STATUS_OFFLINE, STATUS_CONNECTED].includes(this.status)) {
         this.setStatus(STATUS_CONNECTING);
         let clientToken;
         try {
           clientToken = await this.ensureClientToken();
         } catch (e) {
+          if (generation !== this.connectionGeneration) return;
           console.warn("Failed to get client token", e);
           this.setStatus(STATUS_ERROR);
           let timeout = DELAY_MS_BEFORE_RETRY_TOKEN_REFRESH * Math.min(MAX_BACKOFF_COEFFICIENT, Math.pow(BACKOFF_BASE, this.retries));
@@ -753,7 +759,10 @@ var YSweetProvider2 = class {
           continue;
         }
         for (let i = 0; i < RETRIES_BEFORE_TOKEN_REFRESH; i++) {
-          if (await this.attemptToConnect(clientToken)) {
+          if (generation !== this.connectionGeneration) return;
+          const connected = await this.attemptToConnect(clientToken);
+          if (generation !== this.connectionGeneration) return;
+          if (connected) {
             this.retries = 0;
             break connecting;
           }
@@ -762,8 +771,10 @@ var YSweetProvider2 = class {
           this.reconnectSleeper = new Sleeper(timeout);
           await this.reconnectSleeper.sleep();
         }
+        if (generation !== this.connectionGeneration) return;
         this.clientToken = null;
       }
+    if (generation !== this.connectionGeneration) return;
     this.isConnecting = false;
     if (this.showDebuggerLink && lastDebugUrl !== this.debugUrl) {
       console.log(
@@ -777,10 +788,37 @@ var YSweetProvider2 = class {
     }
   }
   disconnect() {
-    this.setStatus(STATUS_OFFLINE);
+    this.connectionGeneration += 1;
+    this.isConnecting = false;
+    this.clearHeartbeat();
+    this.clearConnectionTimeout();
+    this.reconnectSleeper?.wake();
     if (this.websocket) {
-      this.websocket.close();
+      const websocket = this.websocket;
+      this.websocket = null;
+      websocket.onmessage = null;
+      websocket.onclose = null;
+      websocket.onerror = () => {};
+      // Chromium warns if close() interrupts the opening handshake. Detach now,
+      // then close this socket on open without running the provider's handshake.
+      if (typeof window !== "undefined" && websocket.readyState === this.WebSocketPolyfill.CONNECTING) {
+        websocket.onopen = () => {
+          websocket.onopen = null;
+          websocket.close();
+        };
+      } else {
+        websocket.onopen = null;
+        websocket.close();
+      }
     }
+    this.setStatus(STATUS_OFFLINE);
+    awarenessProtocol.removeAwarenessStates(
+      this.awareness,
+      Array.from(this.awareness.getStates().keys()).filter(
+        (client) => client !== this.doc.clientID
+      ),
+      this
+    );
   }
   bindWebsocket(websocket) {
     if (this.websocket) {
@@ -926,9 +964,7 @@ var YSweetProvider2 = class {
     this.send(toUint8Array(encoder));
   }
   destroy() {
-    if (this.websocket) {
-      this.websocket.close();
-    }
+    this.disconnect();
     if (this.indexedDBProvider) {
       this.indexedDBProvider.destroy();
     }
