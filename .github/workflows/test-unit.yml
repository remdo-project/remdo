name: Tests/Unit

on: push

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/node-test-shared
        with:
          command: npm run test-unit
