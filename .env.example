# shellcheck disable=SC2034
# RemDo environment overrides (minimal).
# Defaults live in config/spec.ts; set only what you need to override.

# Docker/runtime (single-container).
# Password must be at least 10 characters. Example:
# openssl rand -base64 24
BASICAUTH_PASSWORD=
PORT=4000
# Dev (non-docker) can omit COLLAB_SERVER_PORT so it derives to PORT + 4.
COLLAB_SERVER_PORT=8081
