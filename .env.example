# shellcheck disable=SC2034
# RemDo environment overrides (minimal).
# Defaults live in tools/env.defaults.sh; set only what you need to override.

# Docker/runtime (single-container).
# Password must be at least 10 characters. Example:
# openssl rand -base64 24
# Optional; defaults to the current host username.
# AUTH_USER=
AUTH_PASSWORD=
# Tinyauth runs in same-host mode (same external host/port as the app).
# You normally set AUTH_PASSWORD and optionally PORT, then leave the rest unset.
# Optional base host used to derive TINYAUTH_APP_URL when TINYAUTH_APP_URL is unset.
# Hostname-derived defaults for TINYAUTH_APP_URL host (not your browser URL):
# - host remdo -> app.remdo.shared
# - host remdo.shared -> app.remdo.localhost
# - host remdo.prod.example -> app.remdo.localhost
# - host localhost/unknown -> app.remdo.localhost
# PUBLIC_BASE_DOMAIN=app.remdo.shared
# Optional explicit auth URL used by Tinyauth (defaults to http://${PUBLIC_BASE_DOMAIN}:${PORT}).
# If set manually, use the exact public URL users browse.
# Browser access must use this same host; alias hosts are unsupported.
# Example: TINYAUTH_APP_URL=http://app.remdo.shared:4000
# TINYAUTH_APP_URL=
PORT=4000
# Remote scripts.
# Default remote source for tools/remote/*.sh.
# PROD_APP_ADDR=deploy@prod.example.com:~/projects/remdo
#PROD_APP_ADDR=
# Optional extra backup targets for tools/remote/make-backup.sh.
# Define one var per target; script auto-discovers PROD_APP_ADDR_<suffix>.
# Only alphanumeric suffixes are used (for example: eu, us2).
# PROD_APP_ADDR_eu=deploy@eu.example.com:~/projects/remdo
# PROD_APP_ADDR_us2=deploy@us2.example.com:~/projects/remdo
