import { describe, it } from 'vitest';

describe('selection plugin', () => {
  it.todo('snaps cross-note selections to whole notes as described in docs/selection.md');
  it.todo('implements the progressive selection ladder stages');
});
