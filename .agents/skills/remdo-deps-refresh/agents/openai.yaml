interface:
  display_name: "RemDo Deps Refresh"
  short_description: "Dependency refresh participant for RemDo changes"
  default_prompt: "Use $remdo-deps-refresh as a participant after declaring its clean branch and refresh-unit commit authority."
policy:
  allow_implicit_invocation: true
