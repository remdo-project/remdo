interface:
  display_name: "RemDo Deps Refresh"
  short_description: "Autonomous RemDo dependency and tooling refresh"
  default_prompt: "Use $remdo-deps-refresh to refresh every available dependency and tooling pin, repair breakage, defer unsafe updates, and report the result."
policy:
  allow_implicit_invocation: false
