interface:
  display_name: "RemDo Deps Refresh"
  short_description: "Autonomous RemDo dependency refresh loop"
  default_prompt: "Use $remdo-deps-refresh to apply every available RemDo dependency and tooling upgrade, heal breakage, and report the result."
policy:
  allow_implicit_invocation: false
