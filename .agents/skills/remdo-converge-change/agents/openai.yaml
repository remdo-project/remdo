interface:
  display_name: "RemDo Converge Change"
  short_description: "Fix and re-verify a RemDo change"
  default_prompt: "Use $remdo-converge-change to converge the current RemDo change."
policy:
  allow_implicit_invocation: false
