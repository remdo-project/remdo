interface:
  display_name: "RemDo Prepare Change"
  short_description: "Coordinate and deliver a complete RemDo change"
  default_prompt: "Use $remdo-prepare-change as the developer-facing entry to establish, execute, converge, and hand off this RemDo change."
policy:
  allow_implicit_invocation: false
  subagents:
    authorized: true
    use_cases:
      - "fresh-context exploration"
