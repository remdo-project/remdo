interface:
  display_name: "RemDo Feature Flow"
  short_description: "Autonomous RemDo feature workflow"
  default_prompt: "Use $remdo-feature-flow to drive a self-contained RemDo feature from vague idea to approved spec, implementation, review, and local commits."
policy:
  allow_implicit_invocation: false
  subagents:
    authorized: true
    use_cases:
      - "required fresh-context reads"
      - "independent Research or Phase-4 work"
